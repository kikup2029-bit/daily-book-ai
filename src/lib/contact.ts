/**
 * The one address customers can reach.
 *
 * It lives alone in its own module because it appears in the footer, the
 * privacy policy and the terms, and an address that is right in two places out
 * of three is worse than no address at all — someone gets charged, writes to
 * the wrong mailbox, and hears nothing back.
 *
 * Whatever is here must be a real, monitored inbox before the app takes money.
 */
export const CONTACT_EMAIL = "simplebooksai@gmail.com";
